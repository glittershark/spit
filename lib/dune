(ocamllex
 (modules lexer))

(menhir
 (modules menhir_parser))

(rule
 (with-stdout-to
  stdlib_lisp.ml
  (progn
   (echo "let src = {|")
   (cat lisp/stdlib.lisp)
   (echo "|}"))))

(library
 (name spit)
 (libraries core nice_parser)
 (preprocess
  (pps ppx_jane))
 (inline_tests))
